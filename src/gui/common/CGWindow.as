package ui.common {
	
	/**
	 * Модальное окно
	 * 
	 * @version  1.0.2
	 * @author   meps
	 */
	public class CGWindow extends CGLabel {
		
		public function CGWindow(src:* = null, name:String = null) {
			super(src, name);
			// TODO пока чисто формальный класс для дальнейшего расширения
		}
		
	}

}