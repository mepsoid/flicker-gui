package flicker.gui {
	
	/**
	 * Скроллируемый контейнер
	 * 
	 * @version  1.0.1
	 * @author   meps
	 */
	public class CGAreaScroll extends CGResizable {
		
		public function CGAreaScroll(src:* = null, name:String = null) {
			super(src, name);
			
		}
		
	}

}